import styled from '@emotion/styled';
import { rem } from 'polished';

const Main = styled('main')`
  margin-left: auto;
  margin-right: auto;
  max-width: ${rem('1200px')};
  padding-left: 1em;
  padding-right: 1em;
`;

export default Main;
